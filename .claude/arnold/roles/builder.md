# builder（工程师）岗位补充说明 — Mr. Weirdo Jobs

## 测试怎么跑？哪些测试是"关键链路"？

- 全量测试：`npm test`（本项目测试必须**串行**跑，并行会因临时目录互踩而假失败——历史教训）
- 交活红线：说「可以交付 / 已推送」之前，必须把 `.github/workflows/ci.yml` 里的**每一步**都在本地跑一遍全绿，不能只跑 `npm test` 就宣称通过（历史上发生过两次「宣称全绿实际没跑全」被拍板人抓住）
- 主流程冒烟：登记表 ci_smoke.main_chain 那条链路优先保证不断
