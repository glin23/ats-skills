# Mr. Weirdo Jobs — 进度看板

> 记录哪些活干完了、哪些还排着队。问「现在该做什么」看这里。
> （既有的细粒度待办在 docs/BACKLOG.md，本表只管主线级进度。）

## 进行中

- **项目规整**（`docs/active/2026-07-22_project-cleanup_DESIGN.md`）：git 对齐 / 文档三线制 / 死代码隔离 / 文档补账 / 垃圾清理 / 治理补丁六步。

## 排队中

按现行 PRD（`docs/PRD-improvements.md`）第 5 节的优先级，从上往下做：

- **第一优先：投完够格存量 + 提吞吐**。一次完整运行要把所有够格的行投完（当前 246 个 fit≥5 在等）、给 134 个未打分的补分；并行投递清空大队列；**单行缺信息只跳过该行，绝不中止整批**。
- **第二优先：缺信息不中止该行**。一次性建全档案；表单缺非必填字段就默认或跳过；只有真正必填项才停该行，且这类问题一次性集中问。
- **第三优先：降门槛 + 修 Lever**。fit 门槛大幅下调；只硬挡可疑/诈骗与完全不相关；把 Lever 加进 `SUPPORTED_AUTO_PLATFORMS`（一行改动，免费解锁一个平台）。
- **Workday 平台覆盖**：独立 2B 轨道，仅在拿下金融机构合作后才启动。现有 `shared/workday/` 是 v0.7 脚手架、从未实测。
- **`feat/funnel-report-card` 分支去留**（投递漏斗战报卡，420 行未合并功能）：合并还是废弃，等产品拍板。这是唯一没合并进 main 的远端分支。
- **`shared/` 平铺结构是否分组重构**：60+ 文件平铺，但 20 个技能说明、30 个测试文件都写死了路径，搬动风险落在主链路上。规整任务里明确判为"本轮不做"，要做需单独立项 + 全量回归。

## 已完成

- **onboarding 改版**（2026-06-13 上线）：一次性 intake、缺信息归纳压缩、按解锁岗位数排序、求职信生成。原始 PRD 已归档到 `docs/archive/PRD-v3.md`、`docs/archive/PRD-onboarding-ux.md`。
- **公开 alpha 发布准备**：npx 安装入口、发布门禁 `npm run release:alpha`、演示前 `npm run demo:check`。
